[package]
name = "startHack"
version = "0.0.1"
edition = "2024.beta"

[dependencies]
# Sui dependency is auto-resolved by the CLI to match its own version.
# Removed explicit git dep to avoid CLI/framework version mismatch.

[addresses]
startHack = "0x0"
